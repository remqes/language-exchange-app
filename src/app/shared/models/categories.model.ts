export interface Language {
  value: string;
  name: string;
  availability: boolean;
};

export interface Category {
  value: string;
  name: string;
}

export interface Level {
  value: string;
  name: string;
}
